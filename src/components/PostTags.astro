---
interface Props {
  tags: string[];
}

const props = Astro.props;
---

{
  props.tags.length ? (
    <ul class="flex space-x-4">
      {props.tags.map((tag) => (
        <li class="px-3 py-2 border border-neutral-500 text-white text-xs rounded-sm">{tag}</li>
      ))}
    </ul>
  ) : null
}
